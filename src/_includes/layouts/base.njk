<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ profile.name }} · Résumé</title>

    <!-- The ?v= content hash busts the browser cache whenever styles.css changes. -->
    <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}?v={{ cssVersion }}">
</head>
<body>
    {{ content | safe }}
</body>
</html>
